# [Criterion 1][Metric M1][Level L1]
# Why: Local trust store to verify artifact signature in WARN mode before Promote/Deploy.

# Fixed OIDC-issuer GitHub Actions.
# Why: Only certificates issued through GitHub OIDC.
OIDC_ISSUER=https://token.actions.githubusercontent.com

# Certificate Identity PIN for cosign verify.
# Why: Trust exactly the entries made by this workflow file on the main branch of this repository.
# owner/org  : cicd-lab-safecicd
# repo       : safe-safe-cicd-lab
# workflow   : .github/workflows/ga-security-controls.yml
# branch/ref : refs/heads/main
CERT_IDENTITY= cicd-lab-safecicd/safe-safe-cicd-lab/.github/workflows/ga-security-controls.yml@refs/heads/main

